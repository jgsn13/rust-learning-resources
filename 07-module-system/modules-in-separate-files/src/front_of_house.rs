#![allow(unused)]

// NOTE: for child modules we must have a folder with the same name as the parent module with the
// child module files included in that folder.
pub mod hosting;

// NOTE: we can also create modules by creating a folder with the module name and a root file
// called mod.rs.
pub mod serving;
